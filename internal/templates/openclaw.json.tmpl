{
  "gateway": {
    "port": 18789,
    "auth": {
      "token": "{{.GatewayToken}}"
    },
    "controlUi": {
      "allowedOrigins": ["*"]
    }
  },
  "models": {
    "providers": {
      "{{.ProviderName}}": {
        "baseUrl": "{{.BaseURL}}",
        "api": "openai-completions",
        "apiKey": "{{.APIKeyRef}}",
        "models": [{{range $i, $m := .Models}}{{if $i}},{{end}}{"id": "{{$m.ID}}", "name": "{{$m.Name}}"}{{end}}]
      }
    }
  },
  "agents": {
    "defaults": {
      "model": {
        "primary": "{{.DefaultModel}}"
      }
    }
  }
}
